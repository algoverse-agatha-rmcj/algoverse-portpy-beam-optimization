__pycache__/
*.pyc
*.cache.json
.ipynb_checkpoints/

# plot_dvh.py --cache: full-resolution DVH curve points, rebuilt from the dose data.
# The beams/objectives they illustrate are already in clinical_compare_*.json.
*.npz
results/batch/*.log
results/batch/*.pid

# Re-solve side files from the objective-term backfill. Their objective_terms were verified
# against the committed objectives and merged into *_full_resolution.json.
*_full_resolution_rescored.json
